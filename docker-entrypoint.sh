#!/bin/sh
# Миграции прогоняются на каждом старте: они идемпотентны, а при деплое
# новой версии схема должна догнать код до того, как контейнер начнёт отвечать.
set -e

echo "==> Применяю миграции"
npx medusa db:migrate

echo "==> Запускаю Medusa"
exec npx medusa start
