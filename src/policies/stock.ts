import { definePolicies } from "@medusajs/framework/utils"

/**
 * Права на склад.
 *
 * Правка остатка вынесена из штатных маршрутов ядра в свой эндпоинт
 * (`/admin/stock/level`), а `/admin/inventory-items*` закрыт на запись —
 * иначе остаток можно было бы поменять мимо раздела «Склад», из штатного
 * экрана Inventory или прямым запросом.
 *
 * Своя область прав нужна, потому что при включённом rbac маршрут без
 * объявленной политики открыт всякому, кто вошёл в админку, — в том числе
 * кассиру, которому менять остатки незачем.
 *
 * Объявлять политики нужно именно здесь: при каждом старте модуль ролей
 * сверяет таблицу с этим каталогом и всё, чего в нём нет, удаляет.
 */
export const stockPolicies = definePolicies([
  {
    name: "WriteStock",
    resource: "stock",
    operation: "write",
    description: "Изменение остатков на складе",
  },
])
